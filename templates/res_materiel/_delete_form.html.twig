<form method="post" action="{{ path('res_materiel_delete', {'id': res_materiel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ res_materiel.id) }}">
    <a class="btn btn-lg btn-primary" href="{{ path('res_materiel_index') }}">Retour aux materiels</a>
    <a class="btn btn-lg btn-success" href="{{ path('res_materiel_edit', {'id': res_materiel.id}) }}">Modifier</a>
    <button class="btn btn-lg btn-danger">Supprimer</button>
</form>
